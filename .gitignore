# Local-only Claude / editor config (see CLAUDE.md "Local vs shared")
CLAUDE.local.md
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local-only project log + internal review reports (not part of the public repo)
MEMORY.md
documentation/reviews/

# OS / editor cruft
.DS_Store
*.swp

# Local env
.env.local
*.local.env

# mainnet deploy working dir (tx drafts, refs.json)
scripts/work-mainnet/

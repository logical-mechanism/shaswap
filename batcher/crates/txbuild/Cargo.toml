[package]
name = "txbuild"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "ShaSwap settlement transaction builder — lowers a solver-core Settlement into a Plutus-v3 Conway transaction the validators accept."

[dependencies]
solver-core = { path = "../solver-core" }
pallas-primitives = "1.0.0"
pallas-codec = "1.0.0"
pallas-crypto = "1.0.0"
pallas-addresses = "1.0.0"

[lints]
workspace = true

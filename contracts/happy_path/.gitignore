# Transaction drafts, fetched protocol params, deployment.json — machine-local,
# regenerated by the scripts. Script envelopes + addresses ARE committed.
work/

# Machine-local path overrides sourced by env.sh (real node/keys locations).
env.local.sh
